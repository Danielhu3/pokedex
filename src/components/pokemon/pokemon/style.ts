import styled from "styled-components";
export const Pokemon = styled.div`
    display: flex;
    
    position: relative;
    & > img {
        margin: 0 auto;
        margin-bottom: -5rem;
        z-index: 2;
        
    }
`