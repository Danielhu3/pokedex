import styled from "styled-components";
export const Bottom = styled.div`
    grid-area: bottom;  
    background-color: white;
    margin: 0 0.4rem 0.4rem 0.4rem;
    border-radius: 0.8rem;

    padding: 5.6rem 2rem 4.4rem 2rem;

    display: flex;
    flex-direction: column;
    gap: 1.6rem;
`